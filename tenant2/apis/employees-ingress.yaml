---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: api-ingress-employees
  namespace: tenant2
  annotations:
    hub.traefik.io/api: employee-api
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`api.tenant2.dbl25tst3w18.mageekbox.eu`) && PathPrefix(`/employees`)
      services:
        - name: employee-app-latency
          port: 3000
  tls: {}
