---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: hub-dashboard
  namespace: tenant1
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`hub.tenant1.${CLUSTERNAME}.${DOMAINNAME}`)
      services:
        - name: api@internal
          kind: TraefikService
  tls: {}